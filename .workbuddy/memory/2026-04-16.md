# 2026-04-16 工作日志

## 力导向关系图（Graph）实现
- 创建 `src/components/graph.vue` 组件，使用 ECharts 的 force 图表类型实现人物关系网络
- 8个节点（A-H），分男性（绿色#009800）和女性（蓝色#4592ff）
- 9条关系连线：夫妻、父子、朋友、同事
- **修复**：ECharts option 必须用 `series` 数组包裹，否则图表空白
- 配置项：力导向布局、focusNodeAdjacency 悬浮高亮、curveness=0.3 曲线边、repulsion=1000 斥力

## 完整组件目录补齐
用户要求按截图左侧目录结构创建全部 17 个 ECharts 图表组件：
bar.vue, line.vue, pie.vue, radar.vue, scatter.vue, stackBar.vue, stackLine.vue,
sunburst.vue, tree.vue, funnel.vue, dataZoom.vue, gauge.vue, heatmap.vue,
timeline.vue, wordCloud.vue, boxplot.vue, graph.vue
另创建了 `src/treeData.json` 树形数据文件。
